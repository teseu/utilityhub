---
const { name, description, url, icon } = Astro.props
---

<div class="card bg-base-100 shadow-lg shadow-primary/10 hover:shadow-xl hover:shadow-primary/30 transition-shadow duration-300 ease-in-out min-h-[200px]">
  <div class="card-body flex flex-col justify-between">
    <div>
      <h2 class="card-title flex items-center gap-2">
        {icon && <Fragment set:html={icon} />}
        {name}
      </h2>
      <p class="text-base mt-2">{description}</p>
    </div>
    <div class="card-actions justify-end mt-4">
      <a href={url} class="btn btn-primary">Try it</a>
    </div>
  </div>
</div>